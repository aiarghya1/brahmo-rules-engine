# BRAHMO Rules Engine

BFS traversal + 5-check filter pipeline: turns a knowledge graph into the
candidate set for one specific user. Deterministic, zero LLM.

Built feature by feature — see the pull requests.
